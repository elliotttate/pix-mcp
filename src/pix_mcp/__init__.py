"""pix-mcp — Model Context Protocol server for Microsoft PIX (pixtool.exe).

Provides tools for launching, attaching, capturing, and running fine-grained
queries over D3D12 GPU captures (.wpix).
"""

__version__ = "0.1.0"
